/** All basic definitions related directly to the game content. */

// --- Persistent Related ---
export const MAXL_ACC_MOB = 4;
export const MAXL_CARGO_ITEM = 128;
export const MAXL_AFFECT = 32;
export const MAXL_AFFECT_WORLD = 47;
export const MAXL_EQUIP = 18;
export const MAXL_INVENTORY = 60;

export const BAG_UNLOCK_TOKEN_ID = 3467;
/** Lockable bags (the last two), each with one unlock-token slot after the inventory. */
export const BAG_UNLOCK_COUNT = 2;

/** Max character name length (wire field char[18]) */
export const MAX_CHAR_NAME_LENGTH = 18;
